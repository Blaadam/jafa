---
sidebar_position: 1
---

# Workspaces

A **workspace** is the root repository created by `jafa init`. It holds the
shared tooling config and a `projects/` directory containing one or more
projects.

Running `jafa init [target-directory]` creates:

- `default.project.json`, `rokit.toml`, `selene.toml`, `stylua.toml` — shared
  Rojo/Rokit/Selene/StyLua configuration
- `wally/wally.toml` — Wally package manifest, shared by every project in the
  workspace (each project links to `../../wally/Packages`)
- A git repository, if one doesn't already exist
- Tools installed via `rokit install`: Rojo, Wally, StyLua, Lune, Mantle,
  Selene, wally-package-types
- Wally packages installed via `wally install --project-path ./wally`
- A `projects/` directory containing a starter project (see
  [Project structure](./project-structure.mdx))

Everything under `projects/` is a separate Rojo project that shares this one
set of tools and packages, so you don't reinstall or reconfigure them for
every experience.
