commands plan:

1. init -> Creates a Jafa Workspace
2. new -> Creates a new project (resource)
3. build -> Compiles an project
4. open -> Opens a project
5. serve -> Serves a project
6. dev -> builds, opens and serves a project (in order)